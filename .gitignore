node_modules/
dist/
coverage/
*.log
.env
.env.local
*.tsbuildinfo
# 记忆搬家（§23）：导入包用完即焚，任何落盘产物不入库
*.relocation-checkpoint.json
uploads/
# twig-memory 为外部构建上下文（muninn 上游副本，见 README「外部构建上下文」）
# mcp-gateway/ 已并入本仓库（自有轻量实现，见 mcp-gateway/Dockerfile）
/twig-memory/
/skills/

# Zeabur 生成的本地密钥文件，不入库
deploy/secrets.*.env

# 本地备份（backup-local.mjs）：备份产物与连接凭证不入库
backups/
scripts/backup.local.env

# mcp-gateway 注册表快照（dev 默认落在 mcp-gateway/ 下）
dynamic-servers.json
