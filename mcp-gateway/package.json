{
  "name": "mnemosyne-mcp-gateway",
  "version": "1.0.0",
  "description": "Mnemosyne MCP Gateway — lazy MCP client aggregator (stdio + streamable HTTP) with built-in core tools; no DB credentials by design (§5.3)",
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
